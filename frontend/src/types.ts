export enum ShelterTypes {
    FETCH_SHELTERS = "FETCH_SHELTERS", 
    FETCH_SHELTER = "FETCH_SHELTER", 
}
export enum AnimalTypes {
    FETCH_ANIMAL = "FETCH_ANIMAL"
}

export enum AppTypes{
    SHOW_LOADER = "SHOW_LOADER",
    HIDE_LOADER = "HIDE_LOADER"
}